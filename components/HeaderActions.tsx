/**
 * HeaderActions — Solo SyncIndicator (nube) en el headerRight.
 * El engrane de Admin va únicamente en la barra de tabs de abajo.
 */
import SyncIndicator from './SyncIndicator';

export default function HeaderActions() {
  return <SyncIndicator />;
}
